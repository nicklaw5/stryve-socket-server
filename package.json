{
  "name": "stryve-server",
  "description": "The socket.io server application that powers Stryve App.",
  "version": "0.0.1",
  "keywords": [],
  "scripts": {
    "server": "NODE_ENV=production node ./dist/server.js",
    "dev": "npm run build && NODE_ENV=development node ./dist/server.js",
    "build": "./node_modules/babel-cli/bin/babel.js src -d dist"
  },
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-core": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "express": "^4.10.2",
    "socket.io": "^1.4.4",
    "striptags": "^2.1.1",
    "uuid": "^2.0.2"
  }
}
